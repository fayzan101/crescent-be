# Use official Node.js LTS image

FROM node:18-alpine
RUN apk add --no-cache openssl

WORKDIR /usr/src/app

COPY package*.json ./
RUN npm install --omit=dev

COPY . .

CMD ["npx", "prisma", "migrate", "dev", "--name", "auto_migration", "--preview-feature"]
